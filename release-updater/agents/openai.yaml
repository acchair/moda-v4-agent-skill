interface:
  display_name: "Moda Companion 内部更新器"
  short_description: "仅供莫大 Agent 内部检查与升级版本"
  default_prompt: "Use $moda-release-updater only as the internal updater behind $moda-companion."

policy:
  allow_implicit_invocation: false
