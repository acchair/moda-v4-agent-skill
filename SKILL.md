---
name: moda-v4
description: 使用 moda-v4 六层框架分析中国 A 股，采集财务、主营、股东、技术、公告、人气、市场拥挤、行业景气、社交热榜、宏观政策、商品供需和可选搜索证据，生成来源可追溯的固定格式报告。用户提供六位股票代码、股票名称，或要求分析 A 股时使用。
---

# Moda V4 A 股分析

## 运行

1. 股票名称先用本地适配器解析代码；结果为空或有歧义时再明确说明，不猜代码。
2. 在本目录运行：

如果 `.env` 配置本地 SearXNG 或 DuckDuckGo MCP，可以先用跨平台健康检查；没有本地搜索服务时，流水线会按 SearXNG → DuckDuckGo MCP → 公共 DuckDuckGo HTML → DuckDuckGo Lite 逐级回退：

```bash
python3 tools/search_stack.py check --json
python3 tools/search_stack.py start-ddg --json
```

```bash
python tools/run_pipeline.py --stock {六位代码} --name {股票名称}
```

Windows 可将 `python3` 替换为 `python`。OpenMinis、Linux 和 macOS 不依赖 PowerShell；OpenMinis 只需访问可达的 SearXNG/MCP URL，不在手机沙箱内运行 Docker。

只有需要强制重新检查当天共享的市场拥挤度和行业景气原始表时，追加 `--refresh`。

3. 先读 `knowledge/research/pipeline/{code}.json`，只使用本次 `ok: true` 的模块报告。
4. 最终评分以 `knowledge/research/scoring/{code}.md` 为准，结构化结果在 `knowledge/research/scorecards/{code}.json`。
5. 只有用户要求保存文件时，才运行 `tools/export_skill_output.py` 导出完整答复。

## 评分合同

- F1 产业趋势与资本开支：30 分，5 个子项。
- 大时代赛道先采用已验证的申万一级/二级行业映射，再与主营业务共同判断。当前候选方向包括 AI 算力与数据中心、半导体国产替代、商业航天与军工、新能源与储能、汽车电动化与出海、资源与周期、机器人与先进制造、创新药与生命科学、大健康与功能性消费。申万宽口径、概念板块或单个热门词不能独立得分；汽车出海还需出口、海外收入或电动化业务证据。
- F2 股东与筹码：15 分，5 个子项。
- F3 生存能力与龙头：20 分，5 个子项。龙头/核心供应商按行业画像判断：制造/消费侧重市场份额、销量、客户配套和技术；软件/平台侧重用户/合同规模、客户覆盖、技术和续费/平台地位；医药/医疗侧重批件/产品、销售排名、适应症或医院覆盖；金融侧重牌照、资产规模、市场份额和区域覆盖；资源/公用事业侧重储量、产量、区域地位、牌照和供应关系。研报标题中的“龙头”等词只作搜索线索，不直接计分。
- F4 利润兑现路径：15 分，4 个子项。
- F5 低位与困境反转：10 分，包含价格分位、估值、市场冷落、业绩拐点和预期差；价格、估值和关注度按逆向思路判断，越低越有利，但低位本身不等于反转。
- F6 修正项：10 分，包含技术结构 4 分、机构方向 2 分、情绪/拥挤度 2 分、风口催化 2 分；完整明细见 [work.md](work.md)。
- F1-F5 是莫大基本面选股逻辑的量化展开：产业趋势与资本开支、股东筹码、生存能力、利润兑现、低位困境反转。F6 是独立量化修正，不使用网页补分。
- 技术结构直接采用 easy-tdx 技术分析得分；机构方向只由量化选股筛选和投资逻辑追踪判断，两者分开评分。
- F6 已纳入综合 100 分，不得在报告中二次相加。
- 报告唯一主评判字段为 `research_score` 和 `action_rating`；`final_score`、`rating` 和 `legacy_rating` 仅保留在结构化结果中兼容旧工具，不再展示在用户报告正文。
- 研究分只在已知证据范围内归一化，用于研究排序；未知项不加分，也不扣分。覆盖率低于 60% 时仍按研究分给出行动评级，但必须在原因和数据覆盖中提示证据不足。
- 用户可见行动评级只有三档：研究分 `>=70 买入`、`60-69 持有`、`<60 卖出`。ST/退市风险直接为 `卖出`；确认的实控人减持和新鲜高位拥挤最高为 `持有`。旧的 `根/矛/学习仓/不碰` 仅保留在结构化兼容字段。
- 结构化缺失证据标记为 `需人工确认` 并进入定向搜索，不读取旧报告补位；搜索命中可补分，但状态必须为 `网络命中（未核验）`。该状态表示证据不足或搜索未完成，不等于利空，也不自动扣分。
- 接口失败、字段缺失和成功空结果必须分开处理；只有请求成功且结果确为空，质押/解禁才可记为已核验 0。
- 180 日公告必须分页覆盖到日期边界或数据末页；覆盖不完整时，不得据此判定股东稳定、审计安全或零催化。
- 三年价格分位至少需要 720 根有效日 K；单独一个 PB 指标不能取得估值满分。
- 行业景气只交叉验证 F1、F4、F5 的确信度，不新增分值；概念板块不能独立形成 F1 赛道或产业链得分。
- F5 是逆向因子：低价格分位、低估值、低关注和行业冰点提供赔率；业绩拐点、订单/现金流/供需改善才提供反转确认。F5 不再以 F3 分数作硬门槛，F3 单独评价生存能力；最终仓位仍受覆盖率和安全类 Hard Cap 限制。

## Hard Cap

- 控股股东或实控人减持：行动评级最高 `持有`。
- ST 或退市风险：行动评级为 `卖出`。
- 三年价格分位 >80%，且**新鲜有效**的市场拥挤度 >=80%：行动评级最高 `持有`。
- 过期拥挤度只展示，不计修正、不触发 Hard Cap。

## 情绪与风险

- 个股人气、社交热榜和申万二级行业拥挤度必须分开解释。
- 热榜命中只表示关注，不代表利好或利空。
- 异常推广风险检查 8 类信号；只有至少两类独立证据交叉支持，才可判为高风险。
- 未命中异常推广词不等于安全，必须保留这项限制说明。
- 宏观和政策只采用量化宏观数据及政府网公开政策标题，不因单条标题直接改变个股得分。
- 市场拥挤度来自乐咕乐股申万二级行业页面，先映射个股所属申万二级行业，再读取全量行业日缓存；按上海自然日每日检查一次并共享。报告必须显示行业、行业代码、换手率分位数、成交额拥挤度分位数、行业强度、检查日期和真实源数据日期。旧数据或刷新失败的备用缓存不得计分。
- 个股讨论优先使用雪球、东方财富公开接口；直接接口无结果后才使用 SearXNG → DuckDuckGo MCP → 公共 DuckDuckGo HTML。搜索命中统一标记 `网络命中（未核验）`，不依赖浏览器状态。

## 行业景气度

- `industry_prosperity`在供需和宏观之后运行，先将 easy-tdx 行业映射到申万二级和一级；初评完成后再按 F1-F5 缺口运行搜索。
- 财务确认使用乐咕六项申万一级行业同比中位数；供需先行检查订单、价格、库存、产能和 PPI；市场验证检查申万指数相对强度、成交活跃和资金流。
- 网络搜索按财务确认、供需先行、市场验证三层分别补充旁证；每层至少两个独立域名且同向才形成层面状态，仍标记未核验，不覆盖结构化数据。
- 输出状态为 `上行 / 改善 / 中性 / 走弱 / 不可用`，覆盖状态为 `完整 / 部分 / 不可用`。
- 乐咕属于 B 级聚合数据；雪球文章属于 C 级方法线索。两者不能单独确认行业景气或改变评分。

## 搜索验证

- 初评后，F1-F5 中 `需人工确认` 和 `部分覆盖` 的子因子生成定向查询；F6 不进入搜索补分。搜索默认使用 75 秒全局预算、最多 12 个目标、每目标最多 2 个查询；先保障 F1-F5 各一项，再按风险、最大分值、缺口状态和因子优先级分配时间。报告必须显示实际使用时间、目标预算、入选/跳过状态及原因。
- SearXNG 优先，失败、无结果或无相关结果时使用 DuckDuckGo MCP；没有本地后端时自动尝试公共 DuckDuckGo HTML。F1-F5 缺口查询额外覆盖巨潮、沪深北交易所、东方财富、同花顺及主流财经媒体。全部无结果标记 `已搜索未命中`，后端异常标记 `搜索失败，需人工确认`。
- 标题、摘要或正文命中公司/行业与目标指标即可形成未核验线索，不要求来源等级、双域名或完整正文。
- 搜索线索可补充分数，但必须显示实际后端和 `网络命中（未核验）`；结构化已验证值不得被网页结果覆盖。
- 多条结果冲突时按同向数量判断，数量相同时采用排名最高结果并显示冲突。
- 只有结构化或明确核验的控股股东减持、ST 或退市风险才可触发 Hard Cap；未核验网络线索只记录为搜索缺口，未搜索到风险不能反推安全。

## 机构方法

- 机构方法实际为 18 项。只有量化选股筛选与投资逻辑追踪参与 F6 的 2 分机构方向判断；其余方法不改变六层评分或 Hard Cap。
- 默认优先：Comps、财报解读、催化剂日历、投资逻辑追踪、DD 覆盖检查。
- DCF、三表、LBO、并购、单位经济学和组合再平衡只有在所需数据或真实持仓齐全时启用。
- 禁止用市值反推自由现金流，禁止虚构组合仓位。

## 固定输出顺序

最终答复严格跟随本次评分报告，不改成自由散文：

1. 总分、评级、技术信号
2. 一句话结论与六段最终判断
3. easy-tdx 日 K 技术分析（当前价、支撑位、压力位、综合判断、缠论结构及 8 项技术指标）
4. 行业景气度交叉验证
5. 六层图形概览
6. 六层评分卡
7. F1-F6 子因子明细表（得分、判断依据、来源、状态）

“一句话结论与最终判断”必须在全部模块汇总后生成，不用 F1-F6 分数做简单概括，而是以六段形成完整投资判断：投资主张、为什么可能值得买、为什么不能直接买、同行竞争与为什么是它、市场分歧与验证、行动评级与证伪条件。必须说明产业位置、主营和利润兑现、价格估值与市场态度，并明确“投资主张态度”为“优选”或“观察”。只有公司兑现、至少两家直接同行比较、市场预期差和反向验证均有已验证证据时才可写“优选”；任一缺失时必须写“观察”，不得将宽口径估值或网络线索写成直接竞争优势。正式 `research_score`、`action_rating`、Hard Cap、覆盖率、来源状态和 `需人工确认` 不因该态度单独改变。附公司产业链位置及核心上中下游对应表。
8. 舆情、社交热榜与异常推广风险
9. Hard Cap 检查
10. 机构方法交叉验证（机构方向仅用于F6评分，不单独展示方法清单）
11. 睡得着检查
12. 动态纠错触发器
13. 数据覆盖与待确认、免责声明

最终结论必须放在报告开头的“一句话结论与最终判断”，六段均采用“粗体编号标题、空行、正文”，并可使用少量与语义对应的 Emoji 帮助扫读。Emoji 只承担视觉提示，不得替代状态、来源或评分字段。固定覆盖投资主张、买入理由、不能直接买、同行竞争、市场分歧、行动评级和证伪条件。结尾不得重复生成最终结论。使用中文和客观分析师语言。每项判断必须有来源；来源失败或证据不足时写 `需人工确认`。不得模仿人物口吻，不作收益承诺，不构成投资建议。

## 安全

不得打印、保存或提交密码、Token、Cookie、私钥、代理凭据或浏览器登录状态。可选凭据只能从环境变量读取。
